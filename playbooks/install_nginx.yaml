---

- hosts: dev
  become: yes
  tasks:
  - name: this will install nginx on remote
    apt: 
      name : nginx
      state: latest
      
  - name: enable nginx on boot
    ansible.builtin.service:
      name: nginx
      enabled: true
      state: started

...